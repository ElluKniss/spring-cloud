spring:
  datasource:
    url: jdbc:mysql://localhost:3306/orderservice?characterEncoding=utf-8&useSSL=false
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root

  application:
    name: userservice

#  pool:
#    coreSize:5
#    maxSize=20
#    queue-capacity=5
#    keep-alive=60
#    thread-name-prefix=dw-


eureka:
  client:
    service-url:
      defaultZone: http://localhost:9527/eureka/

server:
  port: 8082

logging:
  level:
    com.dw.dao: debug
